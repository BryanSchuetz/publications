guard 'sass', :input => 'styles/sass', :output => 'styles/css', :style => :compressed

guard 'livereload' do
  watch(%r{.+\.(html|liquid)$})
  watch(%r{styles/sass/.+\.scss})
  watch(%r{js/.+\.js})
end
